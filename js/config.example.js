// Copy to js/config.js and fill (or use config.html to override via localStorage)
window.MIEC_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  STORAGE_BUCKET: "photos",
  ALLOW_DOMAIN: "" // e.g., "pm.pt"
};